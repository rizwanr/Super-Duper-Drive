#H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.url=jdbc:h2:mem:drive
spring.datasource.username=sa
spring.datasource.password=pass
spring.datasource.driver-class-name=org.h2.Driver


#By default Spring does not support the HTTP request methods PUT and DELETE and as you have noticed when using the @PutMapping
#  or @DeleteMapping annotations it is as if you are sending to the createNote endpoint which is @PostMapping.



spring.mvc.hiddenmethod.filter.enabled=true